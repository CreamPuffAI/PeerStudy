[project]
name = "peerstudy-backend"
version = "0.1.0"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.23.0",
    "httpx>=0.27.0"
]

[tool.poetry.scripts]
start = "backend.app.main:app"
